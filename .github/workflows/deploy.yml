name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run database migrations
        run: pnpm db:push
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Build server
        run: pnpm build

      - name: Build Expo web
        run: npx expo export --platform web --output-dir web-build

      - name: Upload server artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-dist
          path: dist/
          retention-days: 7

      - name: Upload web artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: web-build/
          retention-days: 7

      # ─── Deploy server ────────────────────────────────────────────────
      # Uncomment and configure one of the blocks below to push the server
      # live on your chosen platform.

      # --- Option A: Deploy to a VPS via SSH ---
      # - name: Deploy server via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.DEPLOY_HOST }}
      #     username: ${{ secrets.DEPLOY_USER }}
      #     key: ${{ secrets.DEPLOY_SSH_KEY }}
      #     script: |
      #       cd /srv/the33rdhouse
      #       git pull
      #       pnpm install --frozen-lockfile --prod
      #       pnpm build
      #       pm2 restart the33rdhouse || pm2 start dist/index.js --name the33rdhouse

      # --- Option B: Deploy to Railway ---
      # (connect the GitHub repo in Railway dashboard — it auto-deploys on push)

      # --- Option C: Deploy to Render ---
      # (connect the GitHub repo in Render dashboard — it auto-deploys on push)

      # ─── Deploy Expo web (static) ──────────────────────────────────────
      # Uncomment to deploy the compiled web app to GitHub Pages.

      # - name: Deploy web to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v4
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     publish_dir: web-build
